import { Layout } from '@components/common'
import React from 'react'

const VideoPlayer = () => {
  return (
    <Layout>
      <div>VideoPlayer</div>
    </Layout>
  )
}

export default VideoPlayer
